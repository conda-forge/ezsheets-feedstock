{% set name = "ezsheets" %}
{% set version = "2023.8.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/EZSheets-{{ version }}.tar.gz
  sha256: da3b76f772885fa4be182143b84e3e04508579a04171992b097ff2ab2f669782

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.4
  run:
    - python >=3.4
    - google-api-python-client
    - google-auth-oauthlib
    - oauth2client

test:
  imports:
    - ezsheets
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/asweigart/ezsheets
  summary: A Pythonic interface to the Google Sheets API that actually works as of August 2021.
  license: GPL-3.0-or-later
  license_file:
    - LICENSE.txt

extra:
  recipe-maintainers:
    - thewchan
